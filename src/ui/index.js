/**
 * resource: src/ui/index.js
 * goal: Export all UI modules for declarative lookup
 * channel: ui-registry
 * handoff: epoch.mm.shady-animator.v1.2.ui.registry
 */

import { RegionSelector } from './region-selector.js';

export const modules = {
  RegionSelector
};